# Tests y Docker

La documentación de tests (cómo funcionan, cuándo usar Docker, comandos) está centralizada en:

**`Documentacion/Siguientes_Pasos/Pruebas/TESTS_Y_DOCKER.md`** (desde la raíz del repositorio).

Comando rápido sin Docker (desde esta carpeta):

```bash
mvn test -Dtest=InventarioDocumentalUseCaseTest,CatalogoUseCaseTest,SerieDocumentalUseCaseTest,SubserieDocumentalUseCaseTest,ApiMetaRequestIdTest
```
